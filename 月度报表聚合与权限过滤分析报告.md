# 月度报表汇总数据聚合与权限过滤流程分析报告

> 本报告深度解析 Kimai 时间追踪系统中月度报表从原始工时记录到最终展示的全链路流程。

---

## 一、整体架构概览

月度报表系统包含两类主要报表：
| 报表类型 | 控制器 | 路由 | 权限要求 |
|---------|--------|------|---------|
| 多用户月度列表 | [ReportUsersMonthController](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/src/Controller/Reporting/ReportUsersMonthController.php) | `/reporting/users/month | `report:other` |
| 单用户月度明细 | [UserMonthController](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/src/Controller/Reporting/UserMonthController.php) | `/reporting/user/month` | `report:user` |

**数据流全链路图**：

```
原始工时记录 (kimai2_timesheet 表)
        │
        ▼
[层1] 控制器层 - 请求处理与参数验证
        │  (权限注解 IsGranted)
        ▼
[层2] 用户过滤层 - UserQuery + UserRepository
        │  (可见性过滤 + 团队权限过滤)
        ▼
[层3] 统计查询层 - TimesheetStatisticService
        │  (DQL 聚合查询: SUM + GROUP BY)
        ▼
[层4] 统计模型层 - DailyStatistic / StatisticDate
        │  (按日期维度构建空白骨架 + SQL 结果填充)
        ▼
[层5] 二次聚合层 - prepareReport()
        │  (项目/活动/客户维度的多阶聚合)
        ▼
[层6] 模板渲染层 - Twig
        │  (逐行遍历 + 前端累计汇总)
        ▼
最终展示
```

---

## 二、权限过滤机制详解

### 2.1 路由级权限（第一道防线）

**位置**：控制器类级别的 `#[IsGranted]` 注解

- [ReportUsersMonthController#L29-L31](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/src/Controller/Reporting/ReportUsersMonthController.php#L29-L31)
```php
#[Route(path: '/reporting/users')]
#[IsGranted('report:other')]
final class ReportUsersMonthController extends AbstractController
```

- [UserMonthController#L26-L28](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/src/Controller/Reporting/UserMonthController.php#L26-L28)
```php
#[Route(path: '/reporting/user')]
#[IsGranted('report:user')]
final class UserMonthController extends AbstractUserReportController
```

### 2.2 ReportingVoter 权限映射（第二道防线）

**位置**：[ReportingVoter.php](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/src/Voter/ReportingVoter.php)

四种报表权限与基础权限的映射关系：

| 报表权限属性 | 需要的角色权限 | 说明 |
|------------|--------------|------|
| `report:user` | `view_reporting` | 查看自己的报表（基础权限） |
| `report:other` | `view_reporting` + `view_other_reporting` + `view_other_timesheet` | 查看他人报表 |
| `report:customer` | `view_reporting` + `customer_reporting` | 客户维度报表 |
| `report:project` | `view_reporting` + `project_reporting` | 项目维度报表 |

**核心实现**：[ReportingVoter#L48-L84](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/src/Voter/ReportingVoter.php#L48-L84)
```php
protected function voteOnAttribute(string $attribute, mixed $subject, TokenInterface $token): bool
{
    $permissions = ['view_reporting'];  // 基础权限：所有报表都需要

    switch ($attribute) {
        case 'report:other':
            $permissions[] = 'view_other_reporting';
            $permissions[] = 'view_other_timesheet';  // 同时需要查看他人工时权限
            break;
        // ...其他case
    }

    foreach ($permissions as $permission) {
        if (!$this->permissionManager->hasRolePermission($user, $permission)) {
            return false;  // 任一不满足即拒绝
        }
    }
    return true;
}
```

### 2.3 用户列表权限过滤（第三道防线）

**位置**：[UserRepository#addPermissionCriteria](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/src/Repository/UserRepository.php#L206-L258)

在多用户报表中，通过 `UserQuery` 获取可见用户时进行过滤逻辑：

```
条件1：canSeeAllData() → 超级管理员/拥有 view_all_data 权限：不过滤
条件2：用户是团队组长 (isTeamlead()) → 包含：
        - 自己作为组长的所有团队成员
条件3：指定了搜索团队 (SearchTeams) → 包含：
        - 指定团队的所有成员
条件4：始终包含 → 用户自己
```

**多用户报表触发点**：[ReportUsersMonthController#L72-L87](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/src/Controller/Reporting/ReportUsersMonthController.php#L72-L87)
```php
$query = new UserQuery();
$query->setVisibility(VisibilityInterface::SHOW_BOTH);  // 显示启用和禁用用户
$query->setSystemAccount(false);  // 排除系统账户
$query->setCurrentUser($currentUser);  // 设置当前用户用于权限过滤
// ... 可选：按团队过滤
$allUsers = $userRepository->getUsersForQuery($query);
```

### 2.4 单用户切换权限（第四道防线）

**位置**：[UserMonthController#L79-L81](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/src/Controller/Reporting/UserMonthController.php#L79-L81)

```php
if ($currentUser !== $values->getUser() && !$canChangeUser) {
    throw new AccessDeniedException('User is not allowed to see other users timesheet');
}
```

其中 `canChangeUser()` 定义于 [AbstractUserReportController#L29-L37](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/src/Controller/Reporting/AbstractUserReportController.php#L29-L37)，需要同时具备：
- `view_other_timesheet`
- `view_other_reporting`

### 2.5 团队权限补充：RolePermissionManager

**位置**：[RolePermissionManager.php](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/src/Security/RolePermissionManager.php)

核心检查 `checkTeamAccessTimesheet()` 验证链路：

```
TimesheetVoter (单条记录级)
    │
    ├─ 自己的记录 → 检查 xxx_own_timesheet 系列权限
    │
    └─ 他人的记录
         ├─ checkTeamAccessTimesheet() 链路
         │    ├─ 记录的项目是否可通过用户团队访问
         │    │   ├─ 客户团队权限
         │    │   └─ 项目团队权限
         │    ├─ 记录的活动团队访问权限
         │    └─ 用户是否为记录所属用户团队的组长
         │
         └─ 检查 xxx_other_timesheet 系列权限
```

**注意**：在月度报表的统计查询中，团队级别的权限是**未调用 TimesheetVoter 的逐条记录过滤，而是通过**限制查询用户列表**来间接实现权限控制。

---

## 三、数据聚合流程详解

### 3.1 原始数据来源：Timesheet 实体

**位置**：[Timesheet 实体](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/src/Entity/Timesheet.php)

核心字段（用于统计的关键字段）：

| 字段 | 数据库列 | 说明 |
|-----|--------|------|
| `$date` | `date_tz` | 用户时区下的日期(用于时区转换后日期，专门用于统计查询 |
| `$duration` | `duration` | 工时时长（秒） |
| `$rate` | `rate` | 费率金额 |
| `$internalRate` | `internalRate` | 内部费率 |
| `$billable` | `billable` | 是否可计费 |
| `$user` | `user` | 关联用户 |
| `$project` | `project_id` | 关联项目 |
| `$activity` | `activity_id` | 关联活动 |
| `$end` | `end_time` | 结束时间(非空表示已完成) |

### 3.2 多用户月度报表：ReportUsersMonthController

#### 步骤1：参数准备与日期范围确定

**位置**：[ReportUsersMonthController#L57-L105](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/src/Controller/Reporting/ReportUsersMonthController.php#L57-L105)

```php
// 默认使用当月第一天
$values->setDate($dateTimeFactory->getStartOfMonth());
// 表单提交验证后处理团队过滤
// ...
// 日期范围标准化
$start->modify('first day of 00:00:00');   // 当月第一天 00:00
$end->modify('last day of 23:59:59');      // 当月最后一天 23:59
```

#### 步骤2：构建统计查询 TimesheetStatisticQuery

**位置**：[TimesheetStatisticQuery](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/src/Repository/Query/TimesheetStatisticQuery.php)

参数包含三个核心参数：
- `$begin`：开始日期
- `$end`：结束日期
- `$users`：用户数组（来自权限过滤后的用户列表
- `$project`：可选项目过滤

#### 步骤3：SQL级聚合（核心）

**位置**：[TimesheetStatisticService#getDailyStatistics](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/src/Timesheet/TimesheetStatisticService.php#L32-L101)

**DQL 聚合查询（按月/天/用户/可计费 分组：

```sql
SELECT 
    COALESCE(SUM(t.rate), 0) as rate,
    COALESCE(SUM(t.duration), 0) as duration,
    COALESCE(SUM(t.internalRate), 0) as internalRate,
    t.billable as billable,
    IDENTITY(t.user) as user_id,
    DAY(t.date) as day,
    MONTH(t.date) as month,
    YEAR(t.date) as year,
FROM kimai2_timesheet t
WHERE t.date BETWEEN :begin AND :end
  AND t.user IN (:user_list)
  AND t.end IS NOT NULL   -- 只统计已结束的记录
GROUP BY year, month, day, user, billable
```

**分组维度**：年 + 月 + 日 + 用户 + 可计费

**关键代码要点**：
1. `t.date` 字段（用户时区的日期列
2. 只包含 `end IS NOT NULL`（已结束）的记录
3. 按 billable 分组意味着可计费和不可计费数据分成两行（后续代码合并处理可计费汇总）

#### 步骤4：统计模型骨架构建

**位置**：[DailyStatistic](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/src/Model/DailyStatistic.php)

**先构建全量日期骨架（即使没数据也要显示）：
```php
private function setupDays(): void
{
    $tmp = \DateTime::createFromInterface($this->begin);
    $tmp->setTime(0, 0, 0);
    while ($tmp < $this->end) {
        $id = $tmp->format('Y-m-d');
        $this->days[$id] = new StatisticDate(clone $tmp);
        $tmp->modify('+1 day');
    }
}
```

然后将 SQL 结果填充到骨架中：

```php
foreach ($results as $row) {
    $day = $stats[$row['user']]->getDay($row['year'], $row['month'], $row['day']);
    $day->setTotalDuration($day->getTotalDuration() + (int)$row['duration']);
    $day->setTotalRate(...);
    $day->setTotalInternalRate(...);
    if ($row['billable']) {
        $day->setBillableRate((float)$row['rate']);
        $day->setBillableDuration((int)$row['duration']);
    }
}
```

**StatisticDate 数据结构**：

| 字段 | 类型 | 说明 |
|-----|------|------|
| `totalDuration` | int | 总时长（秒） |
| `totalRate` | float | 总金额 |
| `totalInternalRate` | float | 总内部金额 |
| `billableDuration` | int | 可计费时长 |
| `billableRate` | float | 可计费金额 |
| `amount` | int | 记录数量 |
| `date` | DateTimeInterface | 日期 |

#### 步骤5：模板级二次汇总（Twig 前端累计

**位置**：[user_list_period_data.html.twig](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/templates/reporting/user_list_period_data.html.twig)

模板中进行三重汇总：

```twig
{# 第一层：逐用户累计 #}
{% for userPeriod in stats %}
    {% for period in userPeriod.data %}
        {% set usersTotalDuration = usersTotalDuration + period.totalDuration %}
        {% set totalsDuration[columnKey] = ... %}  {# 纵向累计（按日期 #}
    {% endfor %}
{% endfor %}

{# 第二层：绝对总累计 #}
{% set absoluteDuration = absoluteDuration + period.totalDuration %}

{# 第三层：表格底部：total_summary 行  #}
```

**前端三层汇总维度：

| 层级 | 维度 | 变量 |
|-------|------|------|
| 第一层级 | 每行用户汇总 | usersTotalDuration / usersTotalRate / usersTotalInternalRate|
| 第二层级 | 每列日期汇总 | totalsDuration / totalsRate / totalsInternalRate |
| 第三层级 | 全表绝对汇总 | absoluteDuration / absoluteRate / absoluteInternalRate |

### 3.3 单用户月度报表：UserMonthController

#### 步骤1-2 同上（同多用户报表类似

#### 步骤3：更深维度分组聚合

**位置**：[TimesheetStatisticService#getDailyStatisticsGrouped](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/src/Timesheet/TimesheetStatisticService.php#L107-L181)

**DQL 查询分组维度增加：

```sql
GROUP BY date, project, activity, user, billable
```

查询结果结构：
```
stats[user_id][project_id][activity_id]
                           └─ data: DailyStatistic（按日期的统计）
```

#### 步骤4：prepareReport() 二阶聚合

**位置**：[AbstractUserReportController#prepareReport](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/src/Controller/Reporting/AbstractUserReportController.php#L49-L136)

**三阶聚合过程：

```
一阶：SQL结果 ──► 按活动(project) 聚合 duration/rate/internalRate
二阶：按项目(project) 聚合
              │
              ├─ 加载 Project 实体（从数据库查询）
              ├─ 项目下活动(activities)
              │     └─ 加载 Activity 实体
              │
三阶：按客户(customer) 聚合
                   └─ 聚合项目下所有项目
                   └─ duration/rate/internalRate 总和
```

**三阶后输出数据结构：

```php
$customers = [
    (customerId) => [
        'customer' => Customer 实体,
        'projects' => [
            (projectId) => [
                'project' => Project 实体,
                'duration' => 项目总时长,
                'rate' => 项目总金额,
                'internalRate' => 项目内部总金额,
                'activities' => [
                    (activityId) => [
                        'activity' => Activity 实体,
                        'duration' => 活动总时长,
                        'rate' => 活动总金额,
                        'data' => DailyStatistic（日期维度统计）
                    ]
                ],
                'data' => DailyStatistic（项目维度按日期统计）
            ]
        ],
        'duration' => 客户总时长,
        'rate' => 客户总金额,
        'internalRate' => 客户内部总金额,
    ]
];
```

---

## 四、事件扩展点

### 4.1 查询事件钩子

**位置**：[TimesheetStatisticsQueryEvent](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/src/Event/TimesheetStatisticsQueryEvent.php)

在所有统计查询执行前触发事件：允许插件修改 QueryBuilder：

```php
$this->eventDispatcher->dispatch(new TimesheetStatisticsQueryEvent($qb));
```

---

## 五、关键注意事项

### 5.1 时区处理机制

- `Timesheet 实体中的 `date_tz` 列存的是**用户时区转换后日期**，而非 UTC 日期。这是为了统计查询时按用户本地日期正确分组。

### 5.2 可计费(billable)处理

- SQL分组时 billable 单独分组，后续代码中 billable 金额累加到 billableDuration/billableRate，同时也累加到总 totalDuration/totalRate。即：
- **总时长 = 可计费时长 + 不可计费时长（即`billable=false 的记录

### 5.3 空数据处理

多用户报表中，若无数据，生成一个空的 DailyStatistic，显示无数据提示。

### 5.4 未结束记录

统计时自动忽略 `end IS NULL` 的记录（运行中记录）不参与汇总。

### 5.5 权限与性能优化

- 报表查询通过限制用户列表实现间接权限控制，而非逐条记录检查，避免 N+1 性能问题。

---

## 六、涉及的核心文件清单

| 类别 | 文件路径 |
|-----|---------|
| 控制器（多用户月报 | [ReportUsersMonthController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/src/Controller/Reporting/ReportUsersMonthController.php) |
| 控制器（单用户月报 | [UserMonthController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/src/Controller/Reporting/UserMonthController.php) |
| 抽象报表基类 | [AbstractUserReportController.php](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/src/Controller/Reporting/AbstractUserReportController.php) |
| 统计服务 | [TimesheetStatisticService.php](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/src/Timesheet/TimesheetStatisticService.php) |
| 统计查询模型 | [TimesheetStatisticQuery.php](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/src/Repository/Query/TimesheetStatisticQuery.php) |
| 日统计模型 | [DailyStatistic.php](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/src/Model/DailyStatistic.php) |
| 日期统计日期数据 | [StatisticDate.php](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/src/Model/Statistic/StatisticDate.php) |
| 报表权限投票器 | [ReportingVoter.php](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/src/Voter/ReportingVoter.php) |
| 工时权限投票器 | [TimesheetVoter.php](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/src/Voter/TimesheetVoter.php) |
| 角色权限管理器 | [RolePermissionManager.php](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/src/Security/RolePermissionManager.php) |
| 用户仓库 | [UserRepository.php](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/src/Repository/UserRepository.php) |
| 工时仓库 | [TimesheetRepository.php](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/src/Repository/TimesheetRepository.php) |
| 工时实体 | [Timesheet.php](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/src/Entity/Timesheet.php) |
| 报表列表模板 | [user_list_period_data.html.twig](file:///d:/fz/0601-1/solo-dogfeeding/code/29-kimai/templates/reporting/user_list_period_data.html.twig) |
