# Kimai 层级归属与费率继承机制分析报告

## 1. 层级架构概述

Kimai 采用 **客户(Customer) → 项目(Project) → 活动(Activity)** 的三级自上而下归属层级，工时记录(Timesheet) 同时关联项目和活动，通过层级关系确定归属并继承上层配置。

```
Customer (客户)
    └── Project (项目)  ── 必须归属某一客户
            └── Activity (活动)  ── 可归属某一项目，也可为全局活动
                    └── Timesheet (工时记录)  ── 必须同时指定项目和活动
```

---

## 2. 实体关联关系

### 2.1 核心外键关联

| 实体 | 关联对象 | 关联类型 | 约束 | 代码位置 |
|------|---------|---------|------|---------|
| Project | Customer | ManyToOne | NOT NULL, CASCADE | [Project.php#L55-L61](file:///d:/fz/0601-1/solo-dogfeeding/code/72-kimai/src/Entity/Project.php#L55-L61) |
| Activity | Project | ManyToOne | NULLABLE, CASCADE | [Activity.php#L53-L58](file:///d:/fz/0601-1/solo-dogfeeding/code/72-kimai/src/Entity/Activity.php#L53-L58) |
| Timesheet | Project | ManyToOne | NOT NULL, CASCADE | [Timesheet.php#L153-L159](file:///d:/fz/0601-1/solo-dogfeeding/code/72-kimai/src/Entity/Timesheet.php#L153-L159) |
| Timesheet | Activity | ManyToOne | NOT NULL, CASCADE | [Timesheet.php#L146-L152](file:///d:/fz/0601-1/solo-dogfeeding/code/72-kimai/src/Entity/Timesheet.php#L146-L152) |

### 2.2 全局活动与项目专属活动

Activity 通过 `project` 字段是否为 null 区分类型：

```php
// Activity.php#L188-L191
public function isGlobal(): bool
{
    return $this->project === null;
}
```

- **项目专属活动**：绑定到具体 Project，只能在该项目下使用
- **全局活动**：不绑定 Project (`project = null`)，可被任意允许全局活动的项目使用

项目是否允许使用全局活动由 `globalActivities` 开关控制（默认 `true`），定义于 [Project.php#L168-L172](file:///d:/fz/0601-1/solo-dogfeeding/code/72-kimai/src/Entity/Project.php#L168-L172)。

---

## 3. 工时记录录入时的层级归属校验

### 3.1 校验触发时机

工时记录保存流程：

```
TimesheetService::saveTimesheet()
    → validateTimesheet()  // 触发校验
    → Doctrine onFlush 事件
        → TimesheetSubscriber::onFlush()  // 触发计算器链
```

代码入口位于 [TimesheetService.php#L154-L161](file:///d:/fz/0601-1/solo-dogfeeding/code/72-kimai/src/Timesheet/TimesheetService.php#L154-L161)。

### 3.2 基础归属校验 (TimesheetBasicValidator)

在 [TimesheetBasicValidator.php](file:///d:/fz/0601-1/solo-dogfeeding/code/72-kimai/src/Validator/Constraints/TimesheetBasicValidator.php) 中执行：

1. **项目必填校验**：`project` 不得为 null (L75-L81)
2. **活动必填校验**：根据系统配置 `timesheet.requires_activity` 决定 (L67-L73)
3. **活动与项目一致性校验**：如果活动绑定了项目，则必须与工时记录的项目一致 (L89-L95)
4. **全局活动许可校验**：若项目禁止全局活动 (`globalActivities = false`)，则不允许选择全局活动 (L97-L103)
5. **项目时间范围校验**：工时开始/结束时间必须落在项目的 start-end 日期范围内 (L108-L148)

关键校验代码：

```php
// 活动-项目一致性
if ($hasActivity && null !== $activity->getProject() && $activity->getProject() !== $project) {
    // 抛出 ACTIVITY_PROJECT_MISMATCH_ERROR
}

// 项目禁止全局活动
if ($hasActivity && !$project->isGlobalActivities() && $activity->isGlobal()) {
    // 抛出 PROJECT_DISALLOWS_GLOBAL_ACTIVITY
}
```

### 3.3 可见性层级校验 (TimesheetDeactivatedValidator)

在 [TimesheetDeactivatedValidator.php](file:///d:/fz/0601-1/solo-dogfeeding/code/72-kimai/src/Validator/Constraints/TimesheetDeactivatedValidator.php) 中沿层级向上校验可见性：

```
Activity.isVisible() 必须为 true
    ↓
Project.isVisible() 必须为 true
    ↓
Customer.isVisible() 必须为 true
```

项目可见性的综合判断由 `Project::isVisibleAtDate()` 实现（[Project.php#L441-L457](file:///d:/fz/0601-1/solo-dogfeeding/code/72-kimai/src/Entity/Project.php#L441-L457)），同时检查：
- 项目自身 visible 标志
- 所属客户 visible 标志
- 项目 start/end 时间范围

---

## 4. 费率配置与继承机制

### 4.1 费率配置层级

费率配置分为 4 层，优先级从高到低：

| 优先级 | 层级 | 实体 | Score | 匹配条件 |
|--------|------|------|-------|---------|
| 1 (最高) | 活动费率 | ActivityRate | 5 | 活动匹配 + 用户匹配 |
| 2 | 项目费率 | ProjectRate | 3 | 项目匹配 + 用户匹配 |
| 3 | 客户费率 | CustomerRate | 1 | 客户匹配 + 用户匹配 |
| 4 (最低) | 用户偏好 | UserPreference | - | 用户默认时薪 |

各 Rate 实体定义：
- [ActivityRate.php](file:///d:/fz/0601-1/solo-dogfeeding/code/72-kimai/src/Entity/ActivityRate.php) → Score = 5
- [ProjectRate.php](file:///d:/fz/0601-1/solo-dogfeeding/code/72-kimai/src/Entity/ProjectRate.php) → Score = 3
- [CustomerRate.php](file:///d:/fz/0601-1/solo-dogfeeding/code/72-kimai/src/Entity/CustomerRate.php) → Score = 1

用户偏好的默认时薪定义于 [UserPreference.php#L31-L32](file:///d:/fz/0601-1/solo-dogfeeding/code/72-kimai/src/Entity/UserPreference.php#L31-L32)：
- `HOURLY_RATE = 'hourly_rate'`
- `INTERNAL_RATE = 'internal_rate'`

### 4.2 费率匹配查询

费率匹配由 `TimesheetRepository::findMatchingRates()` 实现（[TimesheetRepository.php#L786-L849](file:///d:/fz/0601-1/solo-dogfeeding/code/72-kimai/src/Repository/TimesheetRepository.php#L786-L849)），通过三次独立查询合并结果：

1. **活动费率查询**：匹配当前活动，用户为当前用户或通用用户（`user = NULL`）
2. **项目费率查询**：匹配当前项目，用户为当前用户或通用用户
3. **客户费率查询**：匹配 `timesheet.project.customer`，用户为当前用户或通用用户

查询条件核心逻辑：
```php
// 同时满足: (当前用户 OR 通用用户) AND (目标实体 OR 通用实体)
$qb->expr()->orX(
    $qb->expr()->eq('r.user', ':user'),
    $qb->expr()->isNull('r.user')
),
$qb->expr()->orX(
    $qb->expr()->eq('r.activity', ':activity'),  // 或 project / customer
    $qb->expr()->isNull('r.activity')
)
```

### 4.3 最佳费率选择算法

在 [RateService.php#L94-L115](file:///d:/fz/0601-1/solo-dogfeeding/code/72-kimai/src/Timesheet/RateService.php#L94-L115) 的 `getBestFittingRate()` 中：

```php
foreach ($rates as $rate) {
    $score = $rate->getScore();
    // 如果费率绑定了具体用户且匹配当前用户，额外 +1 分
    if (null !== $rate->getUser() && $timesheet->getUser() === $rate->getUser()) {
        ++$score;
    }
    $sorted[$score] = $rate;
}
// 按分数升序排列，取最后一个（最高分）
ksort($sorted);
return end($sorted);
```

**最终评分规则**：

| 费率类型 | 基础分 | 用户匹配额外分 | 总分范围 |
|---------|--------|--------------|---------|
| ActivityRate（通用用户） | 5 | +0 | 5 |
| ActivityRate（匹配用户） | 5 | +1 | **6** |
| ProjectRate（通用用户） | 3 | +0 | 3 |
| ProjectRate（匹配用户） | 3 | +1 | 4 |
| CustomerRate（通用用户） | 1 | +0 | 1 |
| CustomerRate（匹配用户） | 1 | +1 | 2 |

> **最高优先级：用户专属的活动费率 (Score=6)**

### 4.4 费率计算流程

费率计算由 [RateCalculator](file:///d:/fz/0601-1/solo-dogfeeding/code/72-kimai/src/Timesheet/Calculator/RateCalculator.php) 在 Doctrine onFlush 事件中触发。

完整计算流程位于 [RateService.php#L31-L92](file:///d:/fz/0601-1/solo-dogfeeding/code/72-kimai/src/Timesheet/RateService.php#L31-L92)：

```
1. 若记录正在运行 (isRunning)，返回 0 费率
2. 获取最佳匹配费率 (getBestFittingRate)
3. 判断费率类型：
   ├─ 固定费率 (isFixed = true)：
   │   └─ 使用 fixedRate，同时继承 internalRate
   └─ 小时费率 (isFixed = false)：
       └─ 使用 hourlyRate，同时继承 internalRate
4. 若无匹配费率，回退到用户偏好中的默认时薪
5. 应用周末/节假日费率因子 (getRateFactor)
6. 计算总费率 = 时薪 × 因子 × (时长 / 3600)
   或直接使用固定费率
```

关键回退逻辑：
```php
// 用户偏好作为最终 fallback
if (null === $hourlyRate) {
    $hourlyRate = (float) $record->getUser()->getPreferenceValue(
        UserPreference::HOURLY_RATE, 0.00, false
    );
}
```

---

## 5. 可计费状态(Billable)的层级继承

在 [BillableCalculator.php](file:///d:/fz/0601-1/solo-dogfeeding/code/72-kimai/src/Timesheet/Calculator/BillableCalculator.php) 中沿层级自下而上确定可计费状态：

```php
$billable = true;

// 1. 检查活动
$activity = $record->getActivity();
if ($activity !== null && !$activity->isBillable()) {
    $billable = false;
}

// 2. 检查项目
$project = $record->getProject();
if ($billable && $project !== null && !$project->isBillable()) {
    $billable = false;
}

// 3. 检查客户
if ($billable && $project !== null) {
    $customer = $project->getCustomer();
    if ($customer !== null && !$customer->isBillable()) {
        $billable = false;
    }
}
```

**继承规则**：任意上层实体标记为不可计费 (`billable = false`)，则工时记录自动变为不可计费，采用 **AND 短路逻辑**。

也支持手动覆盖模式（由 `billableMode` 控制）：
- `BILLABLE_AUTOMATIC` (默认)：按层级自动计算
- `BILLABLE_YES`：强制可计费
- `BILLABLE_NO`：强制不可计费

---

## 6. 预算配置的层级独立

三个层级实体均通过 [BudgetTrait.php](file:///d:/fz/0601-1/solo-dogfeeding/code/72-kimai/src/Entity/BudgetTrait.php) 引入预算字段，但**各自独立**，不存在自动继承：

| 字段 | 类型 | 含义 |
|------|------|------|
| `budget` | float | 金额预算 |
| `timeBudget` | int | 时间预算（秒） |
| `budgetType` | string|null | 预算类型（null=全额，'month'=月度） |

各层级的预算消耗需要独立统计和校验。

---

## 7. 工时记录保存时的计算器链

所有计算由 `TimesheetSubscriber` 在 Doctrine `onFlush` 事件中统一触发（[TimesheetSubscriber.php](file:///d:/fz/0601-1/solo-dogfeeding/code/72-kimai/src/Doctrine/TimesheetSubscriber.php)）。

执行顺序（按 priority 升序）：

| 优先级 | 计算器 | 职责 | 代码位置 |
|--------|--------|------|---------|
| 50 | RateResetCalculator | 若 project/activity/user 变更，重置所有费率 | [RateResetCalculator.php](file:///d:/fz/0601-1/solo-dogfeeding/code/72-kimai/src/Timesheet/Calculator/RateResetCalculator.php) |
| 100 | BillableCalculator | 沿层级计算 billable 状态 | [BillableCalculator.php](file:///d:/fz/0601-1/solo-dogfeeding/code/72-kimai/src/Timesheet/Calculator/BillableCalculator.php) |
| 200 | DurationCalculator | 计算时长并应用舍入规则 | [DurationCalculator.php](file:///d:/fz/0601-1/solo-dogfeeding/code/72-kimai/src/Timesheet/Calculator/DurationCalculator.php) |
| 300 | RateCalculator | 计算费率并写入记录 | [RateCalculator.php](file:///d:/fz/0601-1/solo-dogfeeding/code/72-kimai/src/Timesheet/Calculator/RateCalculator.php) |

> **关键机制**：`RateResetCalculator` (priority=50) 最先执行——当 project、activity 或 user 发生变更时，先调用 `$record->resetRates()` 清空所有费率字段，确保后续重新计算时不会沿用旧值。

---

## 8. 完整数据流总结

### 8.1 工时记录录入流程

```
用户提交工时表单
    ↓
TimesheetService::saveTimesheet()
    ├─ validateTimesheet()
    │   ├─ TimesheetBasicValidator
    │   │   ├─ 校验项目/活动必填
    │   │   ├─ 校验活动-项目归属一致性
    │   │   ├─ 校验项目是否允许全局活动
    │   │   └─ 校验工时时间在项目日期范围内
    │   └─ TimesheetDeactivatedValidator
    │       ├─ 校验 Activity 是否可见
    │       ├─ 校验 Project 是否可见
    │       └─ 校验 Customer 是否可见
    │
    └─ Doctrine persist → onFlush 事件
        └─ TimesheetSubscriber::calculateFields()
            ├─ [50] RateResetCalculator：检测归属变更，重置费率
            ├─ [100] BillableCalculator：层级继承 billable
            ├─ [200] DurationCalculator：计算时长
            └─ [300] RateCalculator：调用 RateService
                ├─ findMatchingRates()：查询活动/项目/客户三级费率
                ├─ getBestFittingRate()：评分选择最佳费率
                ├─ fallback 用户默认时薪
                ├─ 应用费率因子（周末/节假日）
                └─ 计算总费率 → 写入 Timesheet.rate
```

### 8.2 费率决策树

```
工时记录费率计算
    │
    ├─ 是否正在运行？
    │   └─ 是 → 返回 0
    │
    ├─ 手动设置了 fixedRate？
    │   └─ 是 → 直接使用
    │
    ├─ 手动设置了 hourlyRate？
    │   └─ 是 → 用时薪 × 时长
    │
    └─ 自动匹配费率：
        ├─ 查询 ActivityRate（当前活动 + 用户匹配）  Score=5~6
        ├─ 查询 ProjectRate（当前项目 + 用户匹配）   Score=3~4
        ├─ 查询 CustomerRate（所属客户 + 用户匹配）  Score=1~2
        ├─ 取最高 Score 的费率
        ├─ 无匹配 → fallback 用户偏好 hourly_rate
        ├─ 应用周末/节假日因子
        └─ 时薪 × 因子 × 时长
```

---

## 9. 关键设计特点

1. **显式关联而非隐式推断**：工时记录直接存储 `project_id` 和 `activity_id`，客户通过 `project.customer` 关联获取，不做冗余存储。

2. **评分式费率选择**：通过 Score 机制（层级基础分 + 用户匹配加分）优雅地解决了多层级多费率的优先级问题，避免复杂的 if-else 嵌套。

3. **通用费率 + 用户专属费率**：每个费率配置的 `user` 字段可为 null（通用）或具体用户（专属），专属费率优先级更高。

4. **Doctrine 事件驱动的计算链**：利用 `onFlush` 事件 + 优先级计算器链，确保每次实体变更都能自动重新计算费率和可计费状态，无需业务层手动调用。

5. **费率重置机制**：归属变更时先清空再重算，防止费率值不一致。

6. **billable 的级联 AND 逻辑**：任一层级标记不可计费，工时记录即不可计费，符合"最严格约束生效"的业务常识。
